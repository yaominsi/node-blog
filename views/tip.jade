extends layout

block content  
  div.full-wrapper
    div(style="text-align:center;padding-top:20%")
      h1 #{message}
      if (uesr)
        h2 #{user.name}/#{user.pwd}
        //h2 #{error.status)
        //pre #{error.stack}
